- hosts: all
  gather_facts: no
  tasks:
  - name: install python 2 ubuntu
    raw: test -e /usr/bin/python || (yum -y update && yum install -y python-minimal) || (apt -y update && apt install -y python-minimal)
