---
- name: Ensures {{ PATH_STRONGSWAN_CONFIG }} dir exists
  file: path={{ PATH_STRONGSWAN_CONFIG }}  state=directory

- name: Create docker compose for Strongswan
  template:
    src: strongswan-compose.yml.j2
    dest: "{{ PATH_STRONGSWAN_CONFIG }}/strongswan-compose.yml"


