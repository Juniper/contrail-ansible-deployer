---
- hosts:
  - localhost
  tasks:
  - include_role:
      name: vcenter
    when: vcenter_servers is defined

- hosts:
  - hypervisors
  roles:
  - image_builder

- hosts:
  - container_hosts
  roles:
  - configure_container_hosts

- hosts:
  - container_hosts
  roles:
  - create_containers
