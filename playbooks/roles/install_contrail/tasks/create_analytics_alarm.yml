---
- name: create /etc/contrail/analytics_alarm
  file:
    path: /etc/contrail/analytics_alarm
    state: directory
    recurse: yes

- name: Set ENABLE_ANALYTICS_ALARM environment variable in /etc/contrail/common.env
  lineinfile:
    dest: /etc/contrail/common.env
    line: "ENABLE_ANALYTICS_ALARM=True"
  when: roles[instance_name].analytics is defined and roles[instance_name].analytics_alarm is defined

- name: update contrail-alarm-gen image
  shell: "docker pull {{ container_registry }}/{{ item }}:{{ contrail_version_tag }}"
  with_items:
    - contrail-analytics-alarm-gen

- name: update contrail-external-kafka image
  shell: "docker pull {{ container_registry }}/{{ item }}:{{ contrail_version_tag }}"
  with_items:
    - contrail-external-kafka

- name: create contrail analytics_alarm file
  template:
    src: contrail-analytics-alarm.yaml.j2
    dest: /etc/contrail/analytics_alarm/docker-compose.yaml

- name: start contrail analytics_alarm
  docker_service:
    project_src: /etc/contrail/analytics_alarm
