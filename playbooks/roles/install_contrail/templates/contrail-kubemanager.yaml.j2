version: '2'
services:
    image: "{{ container_registry }}/contrail-kubernetes-kube-manager:{{ contrail_version_tag }}"
    network_mode: host
    environment:
      - K8S_TOKEN={{ hostvars[master_inventory].mastertoken }}
    env_file: /etc/contrail/common_kubemanager.env
    volumes:
      - /var/log/contrail:/var/log/contrail
      - /etc/contrail/vrouter:/etc/contrail
    restart: always
