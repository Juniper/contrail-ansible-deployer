---
- name: set instance data
  set_fact:
    instance_data: "{{ instances[hostvars[inventory_hostname].instance_name] }}"
  tags:
   - always

- name: set instance name
  set_fact:
    instance_name: "{{ hostvars[inventory_hostname].instance_name }}"
  tags:
   - always

# TODO: Specify the tasks here

- fail:
   msg: "install_contrail_over_k8s.yml is under development... please don't use it yet""

