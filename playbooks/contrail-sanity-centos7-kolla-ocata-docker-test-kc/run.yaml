- hosts: all
  tasks:
    - name: List running containers
      shell: 'command -v docker > /dev/null && docker ps -a --format "{% raw %}{{.Names}}{% endraw %}"'
      become: true
      register: container_list
      failed_when: false
